entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

log:
  level: {{ traefik_log_level }}

providers:
  file:
    directory: {{ _file_provider_dir }}
    watch: true

certificatesResolvers:
  {{ traefik_certificates_resolvers | to_nice_yaml(indent=2) | trim | indent(2) }}

